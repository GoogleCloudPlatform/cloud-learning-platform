""" Config used for testing in unit tests """
# pylint: disable=line-too-long
import os

API_URL = "http://localhost/assessment-service/api/v1"

TESTING_FOLDER_PATH = os.path.join(os.getcwd(), "testing")
