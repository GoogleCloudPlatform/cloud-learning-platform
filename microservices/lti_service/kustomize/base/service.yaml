apiVersion: v1
kind: Service
metadata:
  name: lti-service
  labels:
    app: lti-service
spec:
  type: NodePort
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: lti-service